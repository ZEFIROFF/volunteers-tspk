import React from 'react';
import s from './Certificate.module.css';

const Certificate = () => {
    return (
        <div className={`${s.certificateCard} shadow`}>
            <p>Сертификат</p>
        </div>
    );
};

export default Certificate;
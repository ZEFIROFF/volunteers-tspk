import Home from "./components/Home";
import Events from "./components/Events";

const routes = [
    {
        path: '/',
        component: Home
    },
    {
        path: '/events',
        component: Events
    }
]

export default routes